[workspace]
members = ["drainpipe", "golf", "infinite_mirror", "norse", "pipeline", "things"]
resolver = "2"

[workspace.dependencies]
regex = "1.11.1"
polars = { version = "0.46.0", features = ["lazy", "rows"] }

[profile.dev]
opt-level = 0
debug = 0  # skip debug info, saves significant space
